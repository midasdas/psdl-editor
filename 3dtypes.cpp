#include "3dtypes.h"
/*
Vector operator-(const Vector& left, const float& right)
{
	float fLength1 = left.Length();
	if (fLength1 == 0) return left;
	float fLength2 = fLength1 - right;
	return left * fLength2 / fLength1;
}
*/